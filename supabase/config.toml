# Supabase CLI configuration — local development.
# Two hosted projects exist (staging + production); linking is per-environment:
#   supabase link --project-ref <STAGING_REF>     (day-to-day)
#   supabase db push                              (applies migrations to linked project)
# See supabase/README.md for the full environment workflow.
project_id = "agma-os"
