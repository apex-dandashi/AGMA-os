packages:
  - apps/*
  - packages/*

# pnpm 11 blocks dependency postinstall scripts by default; these two are
# Next.js internals that legitimately compile native code.
allowBuilds:
  sharp: true
  unrs-resolver: true
