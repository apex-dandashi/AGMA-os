packages:
  - apps/*
  - packages/*

# pnpm 11 blocks dependency postinstall scripts by default; allowlisted:
# sharp/unrs-resolver (Next.js internals), esbuild (vitest).
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
