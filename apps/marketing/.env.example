# Build-time; both values are public by design (RLS is the security layer).
# Set in the Hostinger deployment env vars for staging + production builds.
# Used by: lead form (edge function endpoint) + client logos live-sync.
NEXT_PUBLIC_SUPABASE_URL="https://<project-ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<anon key>"
